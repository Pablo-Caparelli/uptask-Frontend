-instalamos tailwindcss
-npm i react-router-dom
-npm i -D @types/node
-npm i @headlessui/react @heroicons/react
-npm i react-hook-form
-npm i zod

- npm install --save-dev @types/react
- npm i axios

npm i react-toastify

-React Query o Tan Stack Query es una librería para obtener datos del servidor
Se puede utilizar con Fetch API o Axios
-Queries: GET para obtener datos
-Mutations: Se utilizan para crear, actualizar y eliminar datos (POST,PUT,PATCH,DELETE)
npm i @tanstack/react-query

-npm i @tanstack/react-query-devtools

-react-i18next
npm i react-i18next
(recomendación librería de traducción)

AUTENTICACION:

- un usuario se puede autenticar con algo que se recuerde (Email y Password)
- un usuario se puede autenticar con un dispositivo físico (Mensaje SMS o alerta)
- un usuario se puede autenticar con algo físico
  (Huella Digital, Voz o Cara)

npm i @chakra-ui/pin-input

JSON Web Tokens (JWT)
fomato para transmitir información de manera segura entre dos partes
Header / payload / signature
Los JWT son "sin estado (stateless)"
(npm i jsonwebtoken en backend)
(npm i -D @types/jsonwebtoken)
pagina para verificar token: jwt.io

interceptors de axios
se ejecutan antes o después de una petición HTTP

npm i @tailwindcss/forms

dnd kit
npm install @dnd-kit/core
